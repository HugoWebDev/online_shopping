import React from "react";
import BasicBreadcrumbs from "../components/Breadcrumb";

type Props = {};

const Login = (props: Props) => {
  return (
    <div>
      <BasicBreadcrumbs route="login" />
    </div>
  );
};

export default Login;
